import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#b92060',
        },
        secondary: {
            main: '#341c9c',
        }
    },
});

export default theme;